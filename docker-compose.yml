services:
  litellm-proxy:
    build: .
    ports:
      - "4000:4000"
    entrypoint: ["/bin/bash", "/app/entrypoint.sh"]
    volumes:
      - ./src:/app/src
      - ./.env:/app/.env
      - ./entrypoint.sh:/app/entrypoint.sh
    env_file:
      - .env
    restart: unless-stopped
